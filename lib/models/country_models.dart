class CountryModels {
  String label;
  String countryName;
  int noOfTour;
  double rating;
  String url;

  CountryModels(
      {this.label, this.countryName, this.noOfTour, this.rating, this.url});
}
